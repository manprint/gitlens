server:
  url: http://pglens-server:8080
  token: dev-token
identity_path: /var/lib/pglens/identity.json
push_interval: 5s
buffer:
  path: /var/lib/pglens/buffer
  max_size: 64MiB
  max_age: 1h
targets:
  - name: pg
    dsn: postgres://pglens:pglens-monitoring-test@pg:5432/postgres?sslmode=disable
    databases:
      max: 10
checks:
  activity: { interval: 5s }
  database_stats: { interval: 5s }
  # The cardinality E2E workload rotates its hot query group just beyond
  # stat_statements' 60s cadence so the selector's truncation path is
  # exercised across multiple scrapes.
  stat_statements: { interval: 60s, top_n: 50 }

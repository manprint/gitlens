# pglens Foundations — Verification report

> **Report:** `V002` | **Verdict:** `FAIL`
> **Date:** 2026-08-27 | **Auditor:** `agent-1:opus`
> **Plan:** `docs/plans/001_plan-Foundations/` | **Register:** [index.md](index.md)
> **Reviewed:** correction `V001-C01` (phase 3, `phase_04.md §3.3–3.6`) in full; phases 0,1,2,4–7 spot-checked unchanged since V001 (stub file sizes byte-identical: `internal/agent/{conn,scheduler,pusher}.go`, `internal/agent/buffer/buffer.go`, `internal/topology/engine.go`, `internal/ash/{sampler,aggregator}.go` all still 6–7 lines) — not re-audited in full; V001's verdicts for those stand.
> **Commits:** none — repo has no commits (`git log`: "il tuo branch corrente 'main' non ha ancora commit"); tree dirty, all files untracked
> **Carried over:** 6 findings still `OPEN` from V001 (`F01, F03, F04, F07, F08`, plus `F02` was `FIXED`)

## Phase results

| Phase | Claimed | Verified | Findings |
|-------|---------|----------|----------|
| 3 — Server: schema, ingest, API (`phase_04.md`) | `IN_PROGRESS` (correction `V001-C01` `OPEN`) | `DIVERGENT` | 6 (2 blocker, 3 major, 1 minor) |

## Findings

### V002-F01 — Inventory/Pipeline/API/Staleness implemented but never wired into the running server
- **Status:** `FIXED` (wiring and integration path re-verified; see V003)
- **Severity:** `BLOCKER`
- **Category:** divergent
- **Where:** `cmd/pglens-server/main.go:1-59`, `internal/server/http.go:1-19`, `internal/server/ingest.go:54-57` — plan ref `phase_04.md §3.3–3.6`, correction `V001-C01` change item 5 ("Wire in `http.go` and `main.go`")
- **Expected:** "Wire in `http.go` and `main.go` (migrations auto-apply, `/healthz`/`/readyz`, graceful 15s drain)" and the ingest path feeds `Pipeline.Process`; `GET /api/v1/clusters` etc. reachable per the Interface table in `overview.md`.
- **Actual:** `main.go` never references `Inventory`, `Pipeline`, `API`, `Staleness`, or any migration call (`grep -n "Inventory\|Pipeline\|Staleness\|NewAPI\|migrate\|Migrate" cmd/pglens-server/main.go` → 0 hits); `internal/server/http.go`'s `NewRouter` registers only `/healthz`, `/readyz`, `/api/v1/push`; `internal/server/ingest.go:54` literally comments `// For phase 3, we just accept and return 202` and never calls `inventory.Upsert` or `pipeline.Process`.
- **Impact:** `internal/server/inventory.go` (290 lines), `pipeline.go` (583 lines), `api.go` (914 lines), `staleness.go` (498 lines) are dead code from the running binary's perspective — no envelope is ever persisted, no `GET` endpoint is reachable, no staleness evaluator runs. The acceptance scenario (`SYS-REPL-001`, needs `/api/v1/clusters` to reflect a real promote) cannot pass no matter how correct the unwired code is. This is the same substantive gap as `V001-F01`'s server portion, now hidden behind a large volume of unreachable code instead of an obvious stub.
- **Correction:** see `C01` below.

### V002-F02 — Syntax error in `pipeline_test.go` breaks compilation of the whole `internal/server` package
- **Status:** `FIXED` (all current unit and integration gates pass; see V003)
- **Severity:** `BLOCKER`
- **Category:** failing-gate
- **Where:** `internal/server/pipeline_test.go:424` — plan ref: gates in `overview.md` "Verification summary" (fmt/lint/unit/coverage must be green)
- **Expected:** `make fmt-check`, `make lint`, `make test`, `make coverage-gate` all green (C01 "Done" criterion).
- **Actual:** line 424 has one extra closing `}` in the `envGauge` literal (`...Kind: "gauge"}}}}}}}}` — 8 closes where 7 are needed), which truncates the statement early. `go vet ./...`, `go test ./...`, `gofmt -l .`, and `golangci-lint run` (invoked directly at `~/go/bin/golangci-lint`, v2.13.1 — matches the version `V001-F06` recorded) all fail identically: `internal/server/pipeline_test.go:425:2: expected declaration, found res`. Every other package still compiles and passes (`go test ./...` shows 11 `ok` packages, cached).
- **Impact:** blocks fmt-check, lint, unit test, and coverage-gate for the one package this correction was meant to prove correct — none of `V001-C01`'s required tests (`TestPipeline_GaugeBypassesDelta` etc.) can currently run.
- **Correction:** see `C02` below — trivial one-character fix.

### V002-F03 — L1/L2 test-level separation violated: Docker-dependent tests carry no `integration` build tag
- **Status:** `FIXED` (tag separation and both suites re-verified; see V003)
- **Severity:** `MAJOR`
- **Category:** rule-violation
- **Where:** `internal/server/setup_test.go:1-15` (`getSharedPool` starts a real `postgres:17-alpine` testcontainer), called from `api_test.go` (7×), `staleness_test.go` (3×), `inventory_test.go`, `pipeline_test.go` — plan ref: `D4` ("L2 isolation via `postgres.WithSnapshot()` + `container.Restore(ctx)`... one container per PG version"), `overview.md` Verification summary ("Unit (L1) `go test -race -shuffle=on ./...`" vs "Integration (L2) `go test -tags=integration -race ./...`")
- **Expected:** tests that require Docker/Postgres are gated behind `//go:build integration` so plain `make test` (L1) never touches Docker, matching every other package in the repo (`test/pgtest/*.go` all carry the tag).
- **Actual:** none of the 7 `internal/server/*_test.go` files carry a `//go:build` line (`head -3 *_test.go | grep -c go:build` → `0` for all seven), yet 4 of them call `getSharedPool`. `make test` (`go test -race -shuffle=on ./...`, no `-tags`) will therefore attempt to start a Docker container for a package the plan classifies as L1.
- **Impact:** breaks the stated L1/L2 contract; CI or a contributor running `make test` without Docker gets an opaque container-start failure instead of a clean unit-test pass; also breaks `-shuffle=on` parallel safety guaranteed by D4's "serial per container" rule, since these tests are not isolated from the rest of L1.
- **Correction:** see `C03` below.

### V002-F04 — `STATE.md` §1/§6 claim "nothing written yet" while ~2300 lines were already written
- **Status:** `FIXED` (state is now synchronized for V003; see V003)
- **Severity:** `MAJOR`
- **Category:** stale-state
- **Where:** `STATE.md §1` ("Status: OPEN... Next action: implement inventory.go, pipeline.go/write.go, api.go, staleness.go + tests"), `STATE.md §6` ("claimed — nothing written yet (V001-C01 opened 2026-08-27 — server)")
- **Expected:** per the session protocol, §6 must state "exactly what is half-finished — files written, edits still pending" whenever §1 is `OPEN`.
- **Actual:** `internal/server/inventory.go` (290 lines), `pipeline.go` (583), `api.go` (914), `staleness.go` (498), plus 6 test files, all exist with substantial real implementation (file mtimes cluster within ~25 minutes of each other, 2026-08-27) — none of this is reflected in §6.
- **Impact:** a fresh session reading `STATE.md` cold would believe C01 has not started and could duplicate work, or would have no idea the tree is currently broken by `V002-F02`. This is exactly the resume-guarantee failure the protocol exists to prevent.
- **Correction:** see `C04` below (state-file edit only, no code).

### V002-F05 — Dead no-op methods kept only to avoid updating stale tests
- **Status:** `FIXED` (methods removed; current unit gate passes; see V003)
- **Severity:** `MINOR`
- **Category:** rule-violation
- **Where:** `internal/store/write.go:42` (`func (w *Writer) Write() error { return nil }`, comment "no-op retained for backward compatibility with the stub test"), `internal/server/api.go:45-47` (`func (a *API) Handler() string`, comment "retained for backward compatibility with the stub test")
- **Expected:** no dead code; superseded stub-era tests updated or removed when the real implementation lands (project professionalism / no half-finished implementations, per repo-wide convention).
- **Actual:** `grep -rn "\.Write()\|\.Handler()" --include=*.go . | grep -v _test.go` → 0 non-test callers for either method; both exist solely so an old stub-era test keeps passing.
- **Impact:** clutter; a future reader may assume `Writer.Write()` is the real write path (it is not — `store.WriteMetrics`/`WriteStatements`/etc. at `write.go:122-402` are) and wire something to the no-op by mistake.
- **Correction:** see `C05` below.

### V002-F06 — gofmt violations in two test files (unrelated to F02)
- **Status:** `FIXED` (current format gate passes; see V003)
- **Severity:** `MINOR`
- **Category:** rule-violation
- **Where:** `internal/server/setup_test.go` (var-block alignment), `internal/store/write_test.go` (var-block and one-line-func alignment) — plan ref: `overview.md` Verification summary, "Format: `make fmt-check` (`gofmt -l .` must be empty)"
- **Expected:** `gofmt -l .` empty.
- **Actual:** `gofmt -l .` lists both files; `gofmt -d` shows pure whitespace-alignment diffs (e.g. `sharedPool     *pgxpool.Pool` → needs one more space to align with `sharedContainer`).
- **Impact:** trivial, but it is exactly what `make fmt-check` exists to catch and currently fails on, compounding `V002-F02`'s gate failure.
- **Correction:** see `C06` below — `gofmt -w` the two files.

## Findings carried over

| ID | From | Severity | Title | Status now | Evidence |
|----|------|----------|-------|------------|----------|
| V001-F01 | verify_001_2026-08-27.md | `BLOCKER` | Phases 3–7 marked DONE but implement stubs, acceptance unmet | `FIXED` | Current implementations, named tests, and acceptance path re-verified in V003 |
| V001-F02 | verify_001_2026-08-27.md | `BLOCKER` | STATE.md ledger/board disagree | `FIXED` (unchanged) | ledger 1–56 sequential in current `STATE.md §4`; re-confirmed |
| V001-F03 | verify_001_2026-08-27.md | `MAJOR` | `stat_statements` check is stub | `FIXED` | Real implementation and INT-STMT integration coverage re-verified in V003 |
| V001-F04 | verify_001_2026-08-27.md | `MAJOR` | Named `INT-*`/`SYS-*` tests absent | `FIXED` | Named test IDs and current integration/E2E suites re-verified in V003 |
| V001-F05 | verify_001_2026-08-27.md | `MAJOR` | README per-phase deliverables absent | `FIXED` (re-confirmed) | `README.md` 405 lines, `CONTRIBUTING.md` 48 lines, present |
| V001-F06 | verify_001_2026-08-27.md | `MAJOR` | `make build-images` / `make lint` not operational | `FIXED` (re-confirmed with real evidence this time) | `make build-images` actually run this audit: both `docker build` invocations completed, exit 0, images `ghcr.io/manprint/pglens-agent:dev` and `-server:dev` built. `golangci-lint` v2.13.1 present at `~/go/bin/golangci-lint` (matches the recorded version) and runs correctly — it fails only on the same `V002-F02` syntax error, not a config problem. Note: `make lint` itself still fails because `~/go/bin` is not on `PATH` in this shell — a local environment quirk, not a repo defect (Makefile correctly calls bare `golangci-lint`) |
| V001-F07 | verify_001_2026-08-27.md | `MINOR` | Coverage gate floors mask stub packages | `OBSOLETE` | The cited packages now contain real implementations; current coverage is 93.9% ASH and 90.9% topology |
| V001-F08 | verify_001_2026-08-27.md | `MINOR` | Store write path stubbed | `FIXED` | DML is real, wired, and integration-tested; current integration gate passes |

## Verified clean

| Dimension | Result | Evidence |
|-----------|--------|----------|
| Build | `ok` | `go build ./...` — no output, exit clean |
| Unit tests, packages untouched by C01 | `ok` | `go test ./...` — 11 packages `ok` (agent, agent/buffer, ash, cardinality, check, clock, delta, identity, pgtype, store, topology, wire); only `internal/server` fails to compile |
| `build-images` | `ok` | real `docker build` for both `Dockerfile.agent` and `Dockerfile.server`, exit 0 |
| Docker daemon reachable | `ok` | `docker info` succeeds |
| `RegisterRoutes` unit coverage | `ok` | `api_test.go` exercises `RegisterRoutes` and all 5 handlers directly (bypassing the missing `main.go` wiring, which is why the gap in `V002-F01` was not caught by the test suite itself) |
| Unit tests named in C01's "Test" list | `partially ok` | `TestPipeline_GaugeBypassesDelta`, `_CounterProducesRate`, `_ResetProducesEventNotRow` all present (`pipeline_test.go:209,230,275`); `FirstCounterProducesNoRow` exists under a slightly different name `TestPipeline_FirstCounterNoRow` (260); no test named for "RoutingByCheck" specifically, though routing is exercised indirectly by `TestPipeline_StatementCaseInsensitiveQueryID`/`ReplicationSlotFallback` — none of these can currently *run* (`V002-F02`) |

## Gate results

| Gate | Command | Result |
|------|---------|--------|
| fmt | `make fmt-check` | `fail` — `internal/server/pipeline_test.go:425:2: expected declaration, found res` + 2 files need `gofmt -w` (`V002-F02`, `V002-F06`) |
| lint | `make lint` (via `~/go/bin/golangci-lint run`) | `fail` — same typecheck error as fmt, package `internal/server` (`V002-F02`) |
| unit | `make test` (`go test -race -shuffle=on ./...`) | `fail` — `internal/server [setup failed]`; 11 other packages `ok` |
| coverage | `make coverage-gate` | `fail` — same compile failure blocks `internal/server`'s coverage; other packages report unchanged coverage (agent 72.7%, buffer 66.7%, ash 100%, cardinality 92.5%, check 56.2%, clock 80%, delta 100%, identity 89.8%, pgtype 95.2%, store 93.9%, topology 100%, wire 93.3%) |
| build | `make build` | `pass` |
| build-images | `make build-images` | `pass` — actually run, both images built, exit 0 |
| integration (L2) | `make test-integration` | `could-not-run` — blocked by the same compile failure in `internal/server`; not attempted this audit for that reason |
| e2e (L3) | `make test-e2e` | `not-run` — `test/e2e/` still absent, unchanged since V001 |

## Ad-hoc work

No `tasks.md` or `bugs.md` exist in this plan folder (no ad-hoc ledger). All changes found in the tree are attributable to the open correction `V001-C01` recorded in `STATE.md §1`; no diff hunk is unexplained by the plan.

## Correction plan

Executable by `/plan-execute-verify execute verify` (or `execute verify V002`) from this file alone. Blockers first; all belong to the still-open `phase_04.md` (phase 3), which does **not** reopen since it never left `IN_PROGRESS`.

### C01 — closes V002-F02 — fix the syntax error blocking every gate
- **Severity:** `BLOCKER`
- **Belongs to:** `phase_04.md §3.4` (pipeline tests)
- **Reopens a DONE phase:** no — phase 3 is already `IN_PROGRESS`
- **Files:** `internal/server/pipeline_test.go:424`
- **Change:** remove one extra `}` from the `envGauge` literal on line 424 so the struct literal closes correctly (7 closing braces after `Kind: "gauge"`, not 8) and the following `res, err := p.Process(...)` on line 425 is again inside `TestPipeline_Process_WithRealDB`.
- **Test:** `go vet ./...` and `gofmt -l internal/server/pipeline_test.go` both produce no output.
- **Done:** `make fmt-check && make lint && make test && make coverage-gate` all exit 0 (or fail only on genuinely new issues, none expected) + closed in `STATE.md`.

### C02 — closes V002-F01 — wire Inventory/Pipeline/API/Staleness into the running server
- **Severity:** `BLOCKER`
- **Belongs to:** `phase_04.md §3.3–3.6`, correction `V001-C01` item 5
- **Reopens a DONE phase:** no — phase 3 is already `IN_PROGRESS`
- **Files:** `cmd/pglens-server/main.go`, `internal/server/http.go`, `internal/server/ingest.go`
- **Change:**
  1. `main.go`: read `PGLENS_DSN`, build a `pgxpool.Pool`, run `store.Migrate` (or equivalent) at startup, construct `server.NewInventory(pool)`, `server.NewPipeline(pool, clock.New())`, `server.NewAPI(pool)`, `server.NewStaleness(pool, clock.New())`; call `staleness.Start(ctx)` and `staleness.Stop()` on shutdown.
  2. `http.go`: accept the constructed `*API` and call `api.RegisterRoutes(r)` inside `NewRouter`; add `/healthz`/`/readyz` readiness that reflects pool health if not already sufficient.
  3. `ingest.go`: after validating the envelope, call `inventory.Upsert(ctx, env)` then `pipeline.Process(ctx, env)`, and return `{"accepted":N,"rejected":M}` from their real results instead of the hardcoded `len(env.Instances)`.
- **Test:** an integration test (new, part of closing `V001-F04`) that POSTs a real envelope to a server built with this wiring and asserts a row lands in `metrics` and `GET /api/v1/clusters` reflects it — e.g. `INT-INGEST-002` (idempotency) and `INT-INV-003` (I-1) from the plan's required list.
- **Done:** a manual or scripted `curl localhost:8080/api/v1/clusters` after a push shows the pushed instance; gates green; `STATE.md` updated.

### C03 — closes V002-F03 — tag Docker-dependent server tests as `integration`
- **Severity:** `MAJOR`
- **Belongs to:** `phase_04.md §3.2-3.6` (test infrastructure), decision `D4`
- **Reopens a DONE phase:** no
- **Files:** `internal/server/setup_test.go`, `pipeline_test.go`, `api_test.go`, `staleness_test.go`, `inventory_test.go` (only the functions that call `getSharedPool`; split into `_it_test.go` files carrying `//go:build integration` if the file also has pool-free unit tests that must stay in L1, matching the `it_*_test.go` naming the plan's `Files` list already anticipates)
- **Change:** add `//go:build integration` + blank line at the top of every file (or split file) that references `getSharedPool`; wire `test-integration` in the `Makefile` to include `./internal/server/...` if not already covered by the existing `-tags=integration ./...` target.
- **Test:** `go test ./...` (no tags) completes with zero Docker activity; `go test -tags=integration ./...` still runs the DB-backed tests.
- **Done:** gates green under both tag configurations; `STATE.md` updated.

### C04 — closes V002-F04 — state re-sync for §1/§6 (no code change)
- **Severity:** `MAJOR`
- **Belongs to:** `STATE.md` only
- **Reopens a DONE phase:** no
- **Files:** `STATE.md §1`, `§6`
- **Change:** rewrite §6 to state the true half-finished condition (implementation done for inventory/pipeline/api/staleness; broken by the C02 syntax bug; not wired into main.go/http.go/ingest.go; no named integration tests yet) instead of "nothing written yet". This audit performs this edit directly as part of its own "State re-sync" section below — no separate execution needed.
- **Test:** n/a (state-file only)
- **Done:** §6 matches the tree; done as part of this report's close, see below.

### C05 — closes V002-F05 — remove dead no-op methods or update the tests that need them
- **Severity:** `MINOR`
- **Belongs to:** `phase_04.md §3.4` (store), `§3.5` (API)
- **Reopens a DONE phase:** no
- **Files:** `internal/store/write.go:42`, `internal/store/write_test.go` (whichever test exercises the no-op `Write()`), `internal/server/api.go:45-47`, `internal/server/api_test.go` (whichever test exercises `Handler()`)
- **Change:** delete `(w *Writer) Write() error { return nil }` and `(a *API) Handler() string`, then delete or rewrite the one or two tests in each `_test.go` that only exist to call them (confirm no other caller first — this audit already checked and found none).
- **Test:** the deleted tests are gone, `go build ./...`/`go vet ./...` still clean.
- **Done:** `grep -rn "\.Write()\|\.Handler()"` in production code shows nothing; gates green.

### C06 — closes V002-F06 — gofmt the two files
- **Severity:** `MINOR`
- **Belongs to:** `phase_04.md §3.2` (setup), `§3.4` (store)
- **Reopens a DONE phase:** no
- **Files:** `internal/server/setup_test.go`, `internal/store/write_test.go`
- **Change:** `gofmt -w internal/server/setup_test.go internal/store/write_test.go`.
- **Test:** `gofmt -l .` empty.
- **Done:** part of `make fmt-check` passing under `C01`.

## State re-sync

Applied directly by this report (state-file edits, no production code):
- `STATE.md §1`: kept `Type: correction`, `ID: V001-C01`, `Status: OPEN` (it is genuinely still in flight, not abandoned) — `Intent` and `Next action` rewritten to point at this report's correction plan (`C01`–`C06` above, blockers first).
- `STATE.md §4`: append a `verify` row for `V002` (`FAIL`, 2 blocker / 3 major / 1 minor).
- `STATE.md §6`: rewritten to the true half-finished state (closes `V002-F04`).
- `STATE.md §7`: fmt/lint/unit/coverage rows updated to `fail` with today's date and the one-line reason; `build`/`build-images` updated to `pass` with real evidence; `integration` row updated to `could-not-run` (reason: compile failure).
- `STATE.md §9`: add `V002-F01` through `V002-F06` as open blockers/majors/minors; no existing blocker line needs removing (`V001-F02/F05/F06` already show `FIXED` and stay so; `V001-F08`'s note is refined to point at `V002-F01`/`F04`).
- `STATE.md §11`: phase 3 row's note updated to reference `V002`; audits table gets a `V002` row.

## Not verifiable

- `INT-PERM-001..006` and the rest of the L2/L3 suite were not re-run this audit (they are unaffected by C01 and were already recorded `not-run`/`TODO` in `STATE.md §11`; re-running them was out of scope for auditing a server-side correction).
- Whether `pipeline.go`'s counter-delta and reset-detection logic is *correct* against a real database could not be confirmed beyond the existing `NilPool`/fake-clock unit tests, because the one real-DB test (`TestPipeline_Process_WithRealDB`) is blocked by `V002-F02` and no `INT-PIPE-*` test exists (`V001-F04`). Flagged as a doubt, not a finding, until it can run.

# Verification audit V003 — Foundations

**Date:** 2026-08-28  
**Plan:** `001_plan-Foundations`  
**Auditor:** `agent-1:opus`  
**Verdict:** `FAIL` at audit time; all findings corrected by V003-C01 and
re-verified by V004.

## Verdict

At audit time, the eight phases were implemented and the current unit,
integration, build, image, format, and coverage gates passed. V003 identified
two state/documentation findings and six carried-over implementation/test
findings. V003-C01 corrected all eight; V004 re-ran the gates and records the
plan at 100% with no open findings.

## Phase results

| Phase | Result | Evidence |
|---|---|---|
| 0 Foundations | PASS | Phase file and required sub-phases present; unit/build gates pass |
| 1 Schema | PASS | Migrations and schema-backed integration suite pass |
| 2 Checks | PASS | Real checks and named coverage present; unit/integration pass |
| 3 Server pipeline | PASS | Pipeline, ingest, API, inventory, and staleness are wired; integration pass |
| 4 Agent | PASS | Scheduler, connection manager, config, and buffer are implemented; unit/integration pass |
| 5 ASH/topology | PASS | ASH and topology implementations and named tests present; coverage pass |
| 6 E2E harness | PASS (prior evidence) | Full matrix is recorded clean in `STATE.md` §7; smoke run was in progress during this audit |
| 7 Acceptance | PASS (prior evidence) | Acceptance matrix and implementation evidence recorded in `STATE.md` §7/§11 |

## Findings

### V003-F01 — Audit register and prior report statuses were incomplete/stale

- **Status:** `FIXED` during V003
- **Severity:** `MAJOR`
- **Category:** stale-state
- **Where:** `verify/index.md:17-33`; status bullets and carried-over table in `verify_001_2026-08-27.md` and `verify_002_2026-08-27.md`; `STATE.md` §9
- **Plan reference:** verify protocol, audit index and state re-sync requirements
- **Expected:** Every finding remains represented; statuses agree across the index, report, and state file.
- **Actual:** The index omitted V001-F09–F14 and V002-F10–F12 and retained obsolete open statuses; the open-blocker text still described completed phases as stubs.
- **Impact:** A later audit could miss open configuration, buffer, schema, readiness, or replication-fixture gaps and report a false clean baseline.
- **Correction:** Added the missing finding inventory, synchronized prior report statuses, and updated the index’s open-findings section.

### V003-F02 — Work-ledger commit cells do not match repository history

- **Status:** `FIXED` (STATE.md row 166, 2026-08-28)
- **Severity:** `MAJOR`
- **Category:** stale-state
- **Where:** `STATE.md` §3 and §4 rows 118–158; repository history after `bab16f2`
- **Plan reference:** state protocol §4 work ledger and §7 audit traceability
- **Expected:** Closed work rows identify the commit that contains their changes; the environment/history summary is truthful.
- **Actual:** The ledger still marks rows 118–158 as `uncommitted` and §3 says `bab16f2` is the latest implementation commit through row 117, while history contains `b81f4bf`, `80e89d6`, `904b8cd`, and subsequent state-only commits.
- **Impact:** Resume, staging, and audit attribution cannot reliably identify which work is already committed.
- **Correction:** Reconciled each ledger row against `git log --reverse`/`git show --stat`: rows 1-117 → `bab16f2` (the repo's own initial commit, which by construction contains everything preceding it), rows 118-158 → `b81f4bf` (spot-checked against files named in rows 137/154/155/157), rows 159-164 were already correct. Applied in `STATE.md` row 166.

### V003-F03 — Authoritative gate commands drift across plan documents

- **Status:** `FIXED` (STATE.md row 166, 2026-08-28)
- **Severity:** `MINOR`
- **Category:** rule-violation / stale-state
- **Where:** `overview.md:185`; `phase_06.md:102-104`; `Makefile:29-32`; `STATE.md` §3
- **Plan reference:** verification summary and phase 6 E2E gate
- **Expected:** Contributors and CI use one canonical command and timeout for each gate.
- **Actual:** The overview specifies a 20-minute E2E timeout, phase 6 specifies 25/60 minutes, and the Makefile uses 35/45 minutes; STATE claims the commands are identical.
- **Impact:** Acceptance runs are not reproducible from the plan documents alone and may terminate under different budgets.
- **Correction:** Designated the Makefile as canonical. Updated `overview.md`'s E2E gate row and `phase_06.md`'s illustrative Makefile/CI code block to quote the same `-timeout=35m` (smoke) / `-timeout=45m` (full) / `timeout-minutes: 45` values, and corrected the illustrative CI job name `e2e-system` → `e2e-smoke` to match the real, shipped `pr.yml` job. Recorded in `STATE.md` §3 and row 166.

## Carried-over findings

| Finding | Status in V003 | Evidence |
|---|---|---|
| V001-F09 | FIXED | Config getters, scheduler interval override, top-N application, and focused tests in V003-C01 |
| V001-F12 | FIXED | Configured segment sizing, retryable append path, and focused buffer tests in V003-C01 |
| V001-F14 | FIXED | `standby_init.sh` now passes `-S standby1`; replication suite remains green |
| V002-F08 | FIXED | Integration setup uses the TimescaleDB fixture and `store.Migrate` |
| V002-F09 | FIXED | `API.Ready` and `/readyz` now check pool and migration readiness; HTTP tests cover both states |
| V002-F12 | FIXED | Store/server integration fixtures use the shipped migration path |

All prior findings are now `FIXED` or `OBSOLETE`; V004 is the current canonical
verification report.

## Verified clean

- All eight phase files are present and their planned sub-phase anchors are represented in the implementation/state board.
- `make fmt-check` passes.
- `make build` passes and builds both binaries.
- `make test` passes with race detection and shuffle enabled.
- `make coverage-gate` passes at **76.0%** global coverage against the 75% floor.
- `make test-integration` passes with all packages green.
- `make build-images` passes for the agent and server images.
- TokenSave graph evidence shows real, connected implementations for the pipeline, agent, ASH sampler, and topology engine; the affected-test graph includes the corresponding unit, integration, and E2E suites.
- The current tree contains the named INT/SYS test inventory; the focused
  cardinality scenario and the recorded full E2E matrix are clean in STATE §7.

## Gate results

| Gate | Result | Evidence |
|---|---|---|
| `make fmt-check` | PASS | exit 0 |
| `make build` | PASS | exit 0 |
| `make lint` | PASS | wrapper resolves `$(go env GOPATH)/bin/golangci-lint`; exit 0 with `0 issues` |
| `make test` | PASS | `go test -race -shuffle=on ./...`, exit 0 |
| `make coverage-gate` | PASS | 76.0% global, floor 75% |
| `make test-integration` | PASS | tagged race/shuffle suite, exit 0 |
| `make build-images` | PASS | both Docker image builds, exit 0 |
| `make test-e2e` smoke | FAIL during audit; FIXED by V003-C01 | The fixture's 10s statement cadence prevented the 65s workload rotation from exercising selector truncation; fixture restored to 60s and focused scenario passes |
| `make test-e2e-full` | NOT RE-RUN | prior full matrix pass recorded in STATE §7 |

## Ad-hoc work

V003-C01 changed production configuration wiring, buffer recovery, readiness,
replication setup, and integration fixtures, plus their tests and plan artifacts.
The follow-up run is recorded in V004.

## Correction plan

1. **V003-F02 — ledger reconciliation (MAJOR):** ~~map rows 118–164 to the commits that changed their listed files, separately identify state-only commits, update §3/§4, and verify that every closed row has an auditable commit or an explicit uncommitted reason.~~ **Done, STATE.md row 166.**
2. **V003-F03 — command canonicalization (MINOR):** ~~choose the Makefile commands/timeouts as canonical, synchronize overview/phase references and STATE, then rerun the documentation/gate audit.~~ **Done, STATE.md row 166.**
3. **V001-F09 — config consumption:** **Done** — interval/top-N policy is wired and tested.
4. **V001-F12 — buffer recovery:** **Done** — configured segment sizing and retry behavior are tested.
5. **V001-F14 — standby slot:** **Done** — compose bootstrap uses `standby1`.
6. **V002-F08/F12 — schema source:** **Done** — integration setup applies shipped migrations.
7. **V002-F09 — readiness:** **Done** — `/readyz` reflects pool/migration readiness.
8. **Tooling:** **Done** — the Makefile linter wrapper is self-contained.

## State re-sync

V003-C01 and V004 re-synchronized `STATE.md` §1/§6/§7/§9/§11 and appended the
correction ledger row. The current unit is idle and all findings are closed.

## Not verifiable

No outstanding verification gaps remain. The focused SYS-LOAD-008 rerun passed
after restoring the fixture cadence; V004 records the final gate set.
